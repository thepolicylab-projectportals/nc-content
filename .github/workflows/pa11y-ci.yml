name: pa11y accessibility check

on: pull_request

permissions:
  contents: read
  pull-requests: write
  packages: read

jobs:
  call-ci-workflow:
    uses: thepolicylab-projectportals/.github/.github/workflows/pa11y-ci.yml@v3
    secrets: inherit
    with:
      site-name: "projectportal.nc.gov"
